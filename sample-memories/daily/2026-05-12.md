# 2026-05-12

09:20 — Shipped the new search ranking. Deploy went out, then within minutes the API started 500ing on the product detail endpoint. Turned out the code read a `relevance_score` column that the migration had not created yet — the deploy raced ahead of the migration. Rolled back, ran the migration, re-deployed. ~12 minutes of partial downtime.

11:00 — Wrote up the incident. Action item: add a pre-deploy gate that refuses to ship if pending migrations exist. This is the second time a migration-ordering bug has bitten us.

14:30 — Pairing with Uhura on the dashboard charts. The date-range picker was off by one day due to a timezone bug in the boundary calculation.
