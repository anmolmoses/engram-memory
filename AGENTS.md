# Repository instructions

## Personal GitHub publishing (mandatory)

- Author every commit as `Anmol Moses <anmolmoses1998@gmail.com>`.
- Push only to the personal repository `anmolmoses/engram-memory`, directly to `main` unless the user explicitly requests otherwise.
- Never push Engram to `anmolm-growthx` or another GrowthX remote.
- In this shell, use `env -u GITHUB_TOKEN` for GitHub and git write commands so the GrowthX token cannot override the authenticated `anmolmoses` account.
- Run typecheck and the complete test suite before pushing.
