# CLAUDE.md — Engram

## Critical publishing rule

All Engram commits use `Anmol Moses <anmolmoses1998@gmail.com>` and push only to personal `anmolmoses/engram-memory` `main`. Never push to GrowthX. Use `env -u GITHUB_TOKEN` for GitHub and git writes, then run typecheck and all tests before pushing.
